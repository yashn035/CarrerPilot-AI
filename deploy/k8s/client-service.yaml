apiVersion: v1
kind: Service
metadata:
  name: careerpilot-client-svc
  namespace: careerpilot
spec:
  selector:
    app: careerpilot
    component: client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
